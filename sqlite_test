import sqlite3

conn = sqlite3.connect("instance/users.db")
cursor = conn.cursor()

cursor.execute("SELECT username, email, password, is_admin FROM User")
users = cursor.fetchall()

for user in users:
    print(user)

conn.close()
